<%= simple_form_for(@stock_value) do |f| %>
  <%= f.error_notification %>

  <div class="form-inputs">
    <%= f.input :ticker %>
  </div>

  <div class="form-actions">
    <%= f.button :submit, "stock's value" %>
  </div>
<% end %>
